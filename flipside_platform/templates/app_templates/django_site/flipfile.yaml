appName: {{ app_name }}
template: {{ template }}
build:
  function: flipside_platform.build.python_wheel.build
  function_kwargs:
    settings_module: {{ package_name }}.settings
deploy: 
  type: salt
